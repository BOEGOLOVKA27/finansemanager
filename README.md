# FinanseManager

# Последние изменения:
* Изменил отображение класса `card`, теперь блоки не поднимаются вверх, а выделяются тенью другого цвета;
* Изменил отображение класса `section-date`, сделал его таким же, как `section-title`, чтобы стиль был единым;
* Изменил `.container {padding: 0 5px;}` в мобильном представлении;
* Сделал рефакторинг `JS`, связанного с добавлением транзакций (`/transactions/add`);
* Добавил аннотации к коду, с которым взаимодействовал;
* Добавил в `.gitignore` файлы и папки, которые не нужно коммитить;
* Добавил файл `env_example`, в котором указаны переменные окружения, которые нужно установить для запуска проекта;
* Добавил файл `gitignore_example`, в котором указаны файлы и папки, которые нужно игнорировать при коммитах;
